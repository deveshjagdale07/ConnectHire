<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-blue: #4361ee;
            --primary-dark-blue: #3a0ca3;
            --accent-cyan: #4cc9f0;
            --light-bg: linear-gradient(135deg, #f5f7fa 0%, #e4e8f0 100%);
            --card-bg: #ffffff;
            --text-dark: #212529;
            --text-gray: #6c757d;
            --success-color: #4bb543;
            --danger-color: #dc3545;
            --warning-color: #ffc107;
            --border-color: #e9ecef;
            --border-radius: 16px;
            --shadow-sm: 0 2px 10px rgba(0,0,0,0.05);
            --shadow-md: 0 5px 15px rgba(0,0,0,0.08);
            --shadow-lg: 0 10px 25px rgba(0,0,0,0.12);
            --transition: all 0.3s ease;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: var(--light-bg);
            color: var(--text-dark);
            line-height: 1.6;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1000px;
            margin: 40px auto;
            padding: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 50px;
            position: relative;
        }

        .header h1 {
            font-size: 3rem;
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-dark-blue) 100%);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 15px;
            font-weight: 700;
            position: relative;
            display: inline-block;
        }

        .header h1::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 4px;
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-dark-blue) 100%);
            border-radius: 2px;
        }

        .header p {
            color: var(--text-gray);
            font-size: 1.1rem;
            max-width: 600px;
            margin: 0 auto;
        }

        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-card {
            background: var(--card-bg);
            border-radius: var(--border-radius);
            padding: 20px;
            box-shadow: var(--shadow-md);
            text-align: center;
            transition: var(--transition);
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-lg);
        }

        .stat-card i {
            font-size: 2.5rem;
            margin-bottom: 15px;
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-dark-blue) 100%);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .stat-card h3 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 5px;
            color: var(--primary-blue);
        }

        .stat-card p {
            color: var(--text-gray);
            font-size: 0.9rem;
        }

        .application-card {
            background: var(--card-bg);
            border-radius: var(--border-radius);
            padding: 25px;
            margin-bottom: 25px;
            box-shadow: var(--shadow-md);
            display: flex;
            align-items: center;
            gap: 25px;
            transition: var(--transition);
            position: relative;
            overflow: hidden;
            border: none;
        }

        .application-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 5px;
            height: 100%;
            background: linear-gradient(to bottom, var(--primary-blue), var(--accent-cyan));
        }

        .application-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-lg);
        }

        .company-logo-container {
            position: relative;
            flex-shrink: 0;
        }

        .company-logo {
            width: 80px;
            height: 80px;
            border-radius: 16px;
            object-fit: cover;
            border: 3px solid white;
            box-shadow: var(--shadow-md);
            transition: var(--transition);
        }

        .application-card:hover .company-logo {
            transform: scale(1.05);
        }

        .application-details {
            flex: 1;
        }

        .application-details h3 {
            font-size: 1.6rem;
            margin: 0 0 10px 0;
            color: var(--primary-dark-blue);
            font-weight: 600;
        }
        
        .application-details p {
            margin: 8px 0;
            font-size: 1rem;
            color: var(--text-gray);
            display: flex;
            align-items: center;
        }

        .application-details strong {
            color: var(--text-dark);
            min-width: 120px;
            display: inline-block;
        }

        .status-container {
            display: flex;
            align-items: center;
            margin-top: 15px;
        }

        .status-badge {
            display: inline-flex;
            align-items: center;
            padding: 8px 18px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
            text-transform: capitalize;
            color: white;
            box-shadow: var(--shadow-sm);
        }

        .status-badge i {
            margin-right: 8px;
            font-size: 0.8rem;
        }
        
        .status-badge.pending {
            background: linear-gradient(135deg, var(--warning-color) 0%, #e0a800 100%);
        }

        .status-badge.interview_scheduled {
            background: linear-gradient(135deg, var(--accent-cyan) 0%, #3bb3d6 100%);
        }

        .status-badge.hired {
            background: linear-gradient(135deg, var(--success-color) 0%, #3a9e36 100%);
        }

        .status-badge.rejected {
            background: linear-gradient(135deg, var(--danger-color) 0%, #c82333 100%);
        }

        .application-actions {
            display: flex;
            flex-direction: column;
            gap: 10px;
            margin-left: 15px;
        }

        .action-btn {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--light-bg);
            color: var(--primary-blue);
            border: none;
            cursor: pointer;
            transition: var(--transition);
            box-shadow: var(--shadow-sm);
        }

        .action-btn:hover {
            background: var(--primary-blue);
            color: white;
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            background: white;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow-md);
        }

        .empty-state i {
            font-size: 5rem;
            color: var(--primary-blue);
            margin-bottom: 25px;
            opacity: 0.7;
        }

        .empty-state h3 {
            font-size: 2rem;
            color: var(--text-dark);
            margin-bottom: 15px;
        }

        .empty-state p {
            color: var(--text-gray);
            font-size: 1.1rem;
            max-width: 500px;
            margin: 0 auto 30px;
        }

        .browse-jobs-btn {
            display: inline-flex;
            align-items: center;
            padding: 14px 32px;
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-dark-blue) 100%);
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            transition: var(--transition);
            box-shadow: var(--shadow-md);
        }

        .browse-jobs-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(67, 97, 238, 0.3);
        }

        .browse-jobs-btn i {
            margin-right: 10px;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .container {
                padding: 15px;
                margin: 20px auto;
            }
            
            .header h1 {
                font-size: 2.2rem;
            }
            
            .stats-container {
                grid-template-columns: 1fr 1fr;
                gap: 15px;
            }
            
            .application-card {
                flex-direction: column;
                text-align: center;
                gap: 20px;
            }
            
            .application-card::before {
                width: 100%;
                height: 5px;
            }
            
            .application-details p {
                justify-content: center;
            }
            
            .status-container {
                justify-content: center;
            }
            
            .application-actions {
                flex-direction: row;
                margin-left: 0;
                margin-top: 15px;
            }
        }

        @media (max-width: 480px) {
            .stats-container {
                grid-template-columns: 1fr;
            }
            
            .header h1 {
                font-size: 1.8rem;
            }
            
            .stat-card h3 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>My Applications</h1>
            <p>Track and manage all your job applications in one place</p>
        </div>

        <div class="stats-container">
            <div class="stat-card">
                <i class="fas fa-paper-plane"></i>
                <h3><%= applications.length %></h3>
                <p>Total Applications</p>
            </div>
            <div class="stat-card">
                <i class="fas fa-clock"></i>
                <h3><%= applications.filter(app => app.status === 'pending').length %></h3>
                <p>Pending Reviews</p>
            </div>
            <div class="stat-card">
                <i class="fas fa-calendar-check"></i>
                <h3><%= applications.filter(app => app.status === 'interview_scheduled').length %></h3>
                <p>Interviews</p>
            </div>
            <div class="stat-card">
                <i class="fas fa-trophy"></i>
                <h3><%= applications.filter(app => app.status === 'hired').length %></h3>
                <p>Offers Received</p>
            </div>
        </div>

        <% if (applications.length > 0) { %>
            <% applications.forEach(app => { %>
                <div class="application-card">
                    <div class="company-logo-container">
                        <img src="<%= app.company_logo || '/images/default_company.png' %>" alt="<%= app.company_name %> Logo" class="company-logo">
                    </div>
                    
                    <div class="application-details">
                        <h3><%= app.job_role %> at <%= app.company_name %></h3>
                        <p><strong>Compensation:</strong> <%= app.job_compensation %></p>
                        <p><strong>Location:</strong> <%= app.job_location %></p>
                        <p><strong>Applied:</strong> <%= new Date(app.created_at).toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></p>
                        
                        <div class="status-container">
                            <span class="status-badge <%= app.status %>">
                                <% if (app.status === 'pending') { %>
                                    <i class="fas fa-clock"></i>
                                <% } else if (app.status === 'interview_scheduled') { %>
                                    <i class="fas fa-calendar-alt"></i>
                                <% } else if (app.status === 'hired') { %>
                                    <i class="fas fa-trophy"></i>
                                <% } else if (app.status === 'rejected') { %>
                                    <i class="fas fa-times-circle"></i>
                                <% } %>
                                <%= app.status.replace('_', ' ') %>
                            </span>
                            
                            <div class="application-actions">
                                <button class="action-btn" title="View Details">
                                    <i class="fas fa-eye"></i>
                                </button>
                                <button class="action-btn" title="Withdraw Application">
                                    <i class="fas fa-trash-alt"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            <% }); %>
        <% } else { %>
            <div class="empty-state">
                <i class="fas fa-file-alt"></i>
                <h3>No Applications Yet</h3>
                <p>You haven't applied to any jobs yet. Start browsing available positions and submit your first application!</p>
                <a href="/job_seeker/browse_jobs" class="browse-jobs-btn">
                    <i class="fas fa-search"></i> Browse Jobs
                </a>
            </div>
        <% } %>
    </div>

    <script>
        // Simple animation for cards when page loads
        document.addEventListener('DOMContentLoaded', function() {
            const cards = document.querySelectorAll('.application-card');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                
                setTimeout(() => {
                    card.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, 100 + (index * 100));
            });
        });
    </script>
</body>
</html>